# Plane API Configuration
PLANE_API_BASE_URL=https://app.plane.so/api/v1
PLANE_API_KEY=your_secret_token_here
PLANE_WORKSPACE_SLUG=your_workspace_slug_here

# Database Configuration
# For Docker Compose, use 'db' as the host. For local setup, use 'localhost'.
DB_HOST=db
DB_PORT=5432
DB_USER=user
DB_PASSWORD=password
DB_NAME=plane_manager_db

# App Settings
LOG_LEVEL=info